import 'package:flutter/material.dart';

class PostedLetters extends StatelessWidget {
  const PostedLetters({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
