import 'package:flutter/material.dart';

class PostedWorks extends StatelessWidget {
  const PostedWorks({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
