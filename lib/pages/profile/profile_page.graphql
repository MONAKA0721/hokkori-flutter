query MyInfo($userID: ID) {
  posts(where: { hasOwnerWith: { id: $userID } }) {
    totalCount
  }
}

mutation UpdateUser($userID: ID!, $input: UpdateUserInput!) {
  updateUser(id: $userID, input: $input) {
    id
    name
    username
    profile
    avatarURL
  }
}
