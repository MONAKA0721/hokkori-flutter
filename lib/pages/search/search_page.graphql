query LikedPraises($first: Int!) {
  likedPosts(first: $first, type: praise) {
    id
    title
  }
}

query LikedLetters($first: Int!) {
  likedPosts(first: $first, type: letter) {
    ...LetterSummary
  }
}

query SearchCategories($searchText: String) {
  categories(where: { nameContainsFold: $searchText }) {
    edges {
      node {
        id
        name
      }
    }
  }
}

query CategoryPraises($categoryID: ID, $first: Int, $after: Cursor) {
  posts(
    after: $after
    first: $first
    orderBy: { direction: DESC, field: CREATE_TIME }
    where: { type: praise, hasCategoryWith: { id: $categoryID } }
  ) {
    edges {
      node {
        ...PraiseSummary
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}

query CategoryLetters($categoryID: ID, $first: Int, $after: Cursor) {
  posts(
    after: $after
    first: $first
    orderBy: { direction: DESC, field: CREATE_TIME }
    where: { type: letter, hasCategoryWith: { id: $categoryID } }
  ) {
    edges {
      node {
        ...LetterSummary
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
